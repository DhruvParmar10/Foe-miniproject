import React, { useEffect } from 'react'
import supabase from '../config/supabaseClient'

const Products = () => {
  
  return (
    <div>
      <button>
        log out
      </button>
    </div>
    
  )
}

export default Products